

## Redeploy seed-demo-data Edge Function

**Goal**: Redeploy the `seed-demo-data` edge function to pick up the species fallback fix.

**Steps**:
1. Deploy the current code in `supabase/functions/seed-demo-data/index.ts` to the live environment.

No code changes needed — just a redeployment of the existing updated code.

